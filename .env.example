# Ambiente
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change-me

# Banco SQLite (arquivo local)
DATABASE_URL=sqlite:///./usdbrl.db

# Scheduler (em minutos)
FETCH_INTERVAL_MINUTES=30

# Timezone de agendamento (mantém padrão do Recife)
TIMEZONE=America/Recife

# Fontes de câmbio (ordem de tentativa)
# 1) exchangerate.host (gratuito, sem chave)
# 2) awesomeapi (popular no BR)
# 3) fallback estático (último valor salvo)
